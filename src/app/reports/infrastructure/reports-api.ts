/**
 * Reports API service.
 * @remarks
 * This service is not currently used as reports are generated in the store
 * by combining data from multiple sources (products, stock, providers, etc.).
 * Kept for potential future use if backend API endpoints are added.
 */
export class ReportsApi {
  // Reports are generated in ReportsStore by combining data from multiple APIs
}

